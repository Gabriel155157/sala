name: Bot24h
on:
  push:
    branches: [ principal ]
  schedule:
    - cron: '0 */6 * * *'
jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - run: pip install -r requirements.txt
      - name: Rodar Script
        env:
          TELEGRAM_TOKEN: ${{ 7088974821:AAFx0xVtzEnbHleQU7J66wEfVmPtghnRHs0 }}
          CHAT_ID: ${{ -1002270247449 }}
        run: python bot_telegram.py
